{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "Init",
  "title": "",
  "type": "object",
  "properties": {
    "injectable": {
      "type": "boolean",
      "description": "Would you like to use dependency injection for injectable services?",
      "default": true,
      "alias": "di"
    }
  },
  "required": ["injectable"]
}
